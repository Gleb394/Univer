Описание тестового задания

1. Создаем студентов в StudentFactory, группы в GroupFactory и профессора в ProfessorFactory
В проекте есть два профессора(Дмитрий Лазерев, Евгений Хно) и две группы(А, В)

2. Студенты распределяются по группам в FormationGroup.getGroup

3. Студенты выбирают старосту
3.1 В группе есть метод подсчета голосов Group.processElections и переменная voteBasket
корзина, куда каждый бросает свой голос.
3.2 Переменая voteBasket берет значение из метода Student.vote, где random определяет за кого проголосовал студент.
3.3 Метод Group.processElectionsResult определяет за кого было больше всего голосов (возращает Index).
В результате выбирается студент, который победил. Результат передается в обьект elder и выводится в консоль.

4. Профессор делает перекличку группы.
4.1 Так как профессор знает что за группа, с помощью метода Professor.rollCall программа проходим по каждому студенту
из этой группы.
4.2 Профессор спрашивает имя студента (имя вывводится в консоль)
4.3 Явка студента определяется с помощью boolean значения:
если студент есть - в консоль выводиться его имя, фамилия и слово YES
если студента нету - в консоль выводиться прочерк.