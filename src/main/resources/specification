Описание тестового задания

С начала создаются студенты в StudentFactory, группы в GroupFactory и профессора в ProfessorFactory

Потом студенты распределяются по группам в FormationGroup.getGroup

Студенты выбирают старусту, в группе есть метод подсчета голосов Group.processElections, и переменная voteBasket корзина
куда каждый бросает свой голос, переменая voteBasket берет значение из метода Student.vote где random определяет
за кого проголосовал студент, потом метод Group.processElectionsResult определяет за кого было больше всего
(возращает Index) мы получаем студента который победил и записуем его в обьект elder и выводим в консоль.

профессор делает перекличку группы с помощью метода Professor.rollCall, так как профессор знает что за группа, мы
проходим по каждому студенту из этой группы, сначала профессор спрашивает имя студента (имя вывводим в консоль),
явка студента определяется с помощью boolean значения, если студент есть то в консоль выводиться его
имя и фамилия и слова YES, если студента нету то прочерк.